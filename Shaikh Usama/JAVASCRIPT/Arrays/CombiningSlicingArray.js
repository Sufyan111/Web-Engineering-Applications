const first = [1, 2, 3, 4, 5];
const second = [6, 7, 8, 9, 10];

// Using Concat Method
const arr = first.concat(second);
// console.log(arr);

// Using Slice Method

const peice = arr.slice(2, 6);
console.log(peice);

// Elements in Reference type are copied by Reference, and Elements in primitive type are copied by Value
