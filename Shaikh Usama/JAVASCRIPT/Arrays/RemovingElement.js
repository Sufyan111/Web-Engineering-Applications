const numbers = [1, 2, 3, 4, 5, 6, 7, 8];

// End
// numbers.pop();

// Beginning
// numbers.shift();

// Middle
numbers.splice(2, 2);
console.log(numbers);
numbers.splice(2, 2);

console.log(numbers);
